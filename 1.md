1. downlaod ollama

2. Python -m venv .venv 
./venv/Scripts/activate

3. pip install -r requirements.txt

4. ollama pull llama3.2

5. ollama pull mxbai-embed-large

6. create main.py

7. Getting Error: Import "langchain_ollama.llms" could not be resolved. 
    checking 
        i. its installed or not: pip list | findstr langchain
        ii. python -c "from langchain_ollama import OllamaLLM; print('OK')"
        iii. Press Ctrl + Shift + P. Type "Python: Select Interpreter".
             Choose the one that explicitly points to c:\ai\RAG_LANG_CHROMA_OLLAMA\.venv\Scripts\python.exe. (this step resolved the issue. before global
             python path was selected)

8. In command prompt check what are the ollama models available
    ollama list
            NAME                        ID              SIZE      MODIFIED
            mxbai-embed-large:latest    468836162de7    669 MB    2 hours ago
            llama3.2:latest             a80c4f17acd5    2.0 GB    2 hours ago

9. Run main.py: python .\main.py

10. Create .gitignore file and add following folders/files
        .venv/
        __pycache__/
        *.pyc
        # Ignore your ChromaDB folder (usually better to rebuild locally)
        chroma_db/ 
        .env
    run:
        git init
        git add .
        git commit -m "RAG project with LangChain and Chroma using Ollama"


